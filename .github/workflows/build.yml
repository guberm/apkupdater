name: Android Build

on:
  push:
    branches: [main]
    tags: ['3.*']
  pull_request:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Optional existing tag to publish after a successful signed build
        required: false
        type: string

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5

    - name: Setup Java 17
      uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '17'
        cache: 'gradle'

    - name: Build and test pull request
      if: github.event_name == 'pull_request'
      run: ./gradlew test lint assembleDebug

    - name: Configure release signing
      if: github.event_name != 'pull_request'
      env:
        KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
        STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
      run: |
        test -n "$KEYSTORE_BASE64"
        test -n "$STORE_PASSWORD"
        test -n "$KEY_ALIAS"
        test -n "$KEY_PASSWORD"
        printf '%s' "$KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/apkupdater-release.jks"

    - name: Build and test signed release
      if: github.event_name != 'pull_request'
      env:
        APKUPDATER_KEYSTORE_FILE: ${{ runner.temp }}/apkupdater-release.jks
        APKUPDATER_STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
        APKUPDATER_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
        APKUPDATER_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
      run: |
        ./gradlew --no-daemon test lint assembleRelease
        cp ./app/build/outputs/apk/release/com.guberdev.apkupdater-release.apk "$RUNNER_TEMP/com.guberdev.apkupdater-release.apk"
        ./gradlew --no-daemon clean -PsensitiveLogging=true assembleRelease
        mv ./app/build/outputs/apk/release/com.guberdev.apkupdater-release.apk ./app/build/outputs/apk/release/com.guberdev.apkupdater-sensitive-logging-release.apk
        cp "$RUNNER_TEMP/com.guberdev.apkupdater-release.apk" ./app/build/outputs/apk/release/com.guberdev.apkupdater-release.apk

    - name: Verify release signature
      if: github.event_name != 'pull_request'
      run: |
        BUILD_TOOLS="$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -1)"
        STANDARD=./app/build/outputs/apk/release/com.guberdev.apkupdater-release.apk
        DIAGNOSTIC=./app/build/outputs/apk/release/com.guberdev.apkupdater-sensitive-logging-release.apk
        "$BUILD_TOOLS/apksigner" verify --print-certs "$STANDARD"
        "$BUILD_TOOLS/apksigner" verify --print-certs "$DIAGNOSTIC"
        STANDARD_CERT=$("$BUILD_TOOLS/apksigner" verify --print-certs "$STANDARD" | awk -F': ' '/certificate SHA-256 digest/{print $2; exit}')
        DIAGNOSTIC_CERT=$("$BUILD_TOOLS/apksigner" verify --print-certs "$DIAGNOSTIC" | awk -F': ' '/certificate SHA-256 digest/{print $2; exit}')
        test -n "$STANDARD_CERT"
        test "$STANDARD_CERT" = "$DIAGNOSTIC_CERT"

    - name: Remove decoded keystore
      if: always() && github.event_name != 'pull_request'
      run: rm -f "$RUNNER_TEMP/apkupdater-release.jks"

    - name: Create Release
      uses: softprops/action-gh-release@v2
      if: startsWith(github.ref, 'refs/tags/') || inputs.release_tag != ''
      with:
        tag_name: ${{ inputs.release_tag || github.ref_name }}
        name: APKUpdater ${{ inputs.release_tag || github.ref_name }}
        files: |
          ./app/build/outputs/apk/release/com.guberdev.apkupdater-release.apk
          ./app/build/outputs/apk/release/com.guberdev.apkupdater-sensitive-logging-release.apk
        body: |
          - Change the Android application ID to `com.guberdev.apkupdater`; this installs separately from `com.apkupdater` and does not migrate its data.
          - Retry Google Play delivery HTTP 429 failures with bounded authentication refreshes.
          - The sensitive-logging APK includes full Play request URLs and auth/cookie headers for manual debugging. Do not share its logs.
          - Both APKs use the same package, version, and signing certificate.
        prerelease: false
